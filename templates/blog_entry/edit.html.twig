{% extends 'base.html.twig' %}

{% block title %}Edit BlogEntry{% endblock %}

{% block body %}
    <div class="row" >
        <div class="col parent-content">
            <div class="fancy-content" style="width: 60%; margin: auto">
                <div class="form-row">
                    <h1>Edit BlogEntry</h1>

                    {{ include('blog_entry/_form.html.twig', {'button_label': 'Update', 'url': url}) }}

                    <a class="btn btn-dark" href="{{ path('show_entries') }}">back to list</a>

                    {{ include('blog_entry/_delete_form.html.twig') }}

                </div>
            </div>
        </div>
    </div>

{% endblock %}
